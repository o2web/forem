<%= I18n.t('received_reply', :scope => 'forem.topic') %> You can view it here:

<%= forem.forum_topic_url(@topic.forum_id, @topic) %>

To unsubscribe from this topic, use the following link:
<%= forem.unsubscribe_forum_topic_url(@topic.forum, @topic, token: @token) %>
